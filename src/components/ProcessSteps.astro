---
/** Drei Arbeitsschritte – nummeriert, im Linienraster. */
const steps = [
  { nr: '01', title: 'Beratung', text: 'Wir hören zu, verstehen Ihre Wünsche und beraten ehrlich zu Möglichkeiten, Materialien und Aufwand.' },
  { nr: '02', title: 'Planung', text: 'Wir nehmen Maß, stimmen Details ab und planen Ihr Projekt sauber, realistisch und nachvollziehbar.' },
  { nr: '03', title: 'Umsetzung', text: 'Wir setzen Ihr Projekt termintreu, ordentlich und mit hohem handwerklichem Anspruch um.' }
];
---
<ol class="process">
  {steps.map((s) => (
    <li class="reveal">
      <span class="nr" aria-hidden="true">{s.nr}</span>
      <h3>{s.title}</h3>
      <p>{s.text}</p>
    </li>
  ))}
</ol>
<style>
  .process { list-style: none; padding: 0; display: grid; grid-template-columns: repeat(3, 1fr); gap: 0; border: 1px solid var(--line); border-radius: var(--radius); background: var(--paper); }
  .process li { padding: var(--space-4); border-right: 1px solid var(--line); }
  .process li:last-child { border-right: 0; }
  .nr { font-family: var(--font-display); font-size: 2rem; color: var(--brass); display: block; margin-bottom: var(--space-2); font-variant-numeric: tabular-nums; }
  h3 { margin-bottom: var(--space-1); }
  p { color: var(--ink-soft); font-size: var(--text-sm); }
  @media (max-width: 56rem) {
    .process { grid-template-columns: 1fr; }
    .process li { border-right: 0; border-bottom: 1px solid var(--line); }
    .process li:last-child { border-bottom: 0; }
  }
</style>
